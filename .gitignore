.history/
.vscode/

AI_TemplateCreation.md

# Dependencies. ⚠️ THIS WAS MISSING UNTIL 2026-08-12 and 140 files under test/node_modules
# were tracked - dependency bytes in the repo, and after adding a file: dep, an npm symlink committed
# as an EMBEDDED GIT REPOSITORY (which a clone silently cannot fetch). Backend/ was fine only because
# nobody had run npm install there before the first commit.
node_modules/

# Lockfiles — each project generates its own via `npm install`
package-lock.json

# Root run scripts are generated by the wizard (wizard.bat / wizard.sh)
/run_windows.bat
/run_linux.sh

Backend/public/dist/

*.log
.DS_Store
Thumbs.db
# ── THE NOTICING OBSERVATION LOG — DELIBERATELY UNTRACKED ─────────────────────────────────────────────
# Ote, 2026-08-20: "remove it from git tracking and put it in .gitignore. Keep the file locally so the
# experiment can continue, but don't keep another person's private conversation material in repository
# history." It holds Sotera's verbatim account of live conversations, including other people's.
#
# ⚠️ THIS COSTS SOMETHING REAL, so it is stated rather than hidden: git history was the only thing
# proving the generation-1 and generation-2 rows were never edited. That guarantee now comes from the
# generation stamps, the monotonicity assertion in noticing-prompt-purity-check.mjs, and
# noticing-proposals.README.md beside the file. ⛔ Do not re-add it to make provenance easier.
# ⓘ Existing history is NOT rewritten — his explicit call, revisitable separately.
test/results/noticing-proposals.jsonl
